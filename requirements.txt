# 번역 파이프라인이 쓰는 외부 패키지. scripts/bootstrap.py 가 .venv 에 설치한다.
#
# 대시보드(scripts/dashboard.py, launch.py)는 표준 라이브러리만 쓴다.
# 아래 둘은 원문을 만드는 쪽(normalize.py, figures.py, normalize_all.py)에만 필요하다.
# PyMuPDF 는 버전을 고정한다.
#
# 정규화는 이 라이브러리가 텍스트를 어떤 블록·줄로 묶어 주는지에 그대로 기댄다.
# 그래서 버전이 바뀌면 같은 PDF에서 다른 원문이 나온다. 실제로 1.28.2 에서는
#   · 서지 항목이 'Managing Digital Conti-' 에서 잘리고 (다음 줄이 안 붙는다)
#   · 표 제목이 'TABLE 4. EQUIPPING YOUR' / 'FILM COLLECTION' 두 동강이 나며
#   · 색인에서 앞 항목의 쪽수와 다음 표제어가 '46–48 DVD' 로 한 덩어리가 된다
# 1.26.1 에서는 셋 다 올바르게 나온다.
#
# 올릴 때는 반드시 전권을 재생성하고 git diff source/ 로 무엇이 달라졌는지 확인할 것.
# ch01 이 한 글자도 바뀌지 않아야 한다 (사람 감수를 마친 청크라 회귀 감시 기준으로 쓴다).
PyMuPDF==1.26.1
PyYAML>=6.0
