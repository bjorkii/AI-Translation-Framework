# AI 책 번역 파이프라인 — 세션 지침

## 대화 언어

- **사용자와의 대화는 한국어로 한다.** 설명, 코드 주석, 커밋 메시지 모두 한국어를 쓴다.
- 요약(summary)이나 되짚기(recap)는 반드시 한국어로 작성한다.
- 기술 용어와 코드 식별자(함수명·파일명·플래그 등)는 원문 형태를 유지한다.
- **서브에이전트를 띄울 때는 지시문에 "보고서를 한국어로 작성하라"를 명시한다.** 서브에이전트는
  자기만의 새 컨텍스트에서 시작하므로 이 규칙이 자동으로 전달되지 않는다. 읽는 자료가 영문
  코드·영문 원서라 출력 언어가 영어로 끌려간다.
- 다만 Claude Code 자체의 인터페이스 문구(권한 승인 창, 계획 승인 창 등)는 프로그램에 내장된
  것이라 이 파일로 바꿀 수 없다.

## 실행 환경

**모든 스크립트는 `.venv/bin/python`(윈도우는 `.venv\Scripts\python.exe`)으로 돌린다.**
그냥 `python3`로 돌리지 않는다 — 한 컴퓨터에 파이썬이 여러 개 깔려 있는 일이 흔하고,
어느 것을 쓰느냐에 따라 되기도 하고 안 되기도 하는데 실패 메시지는 늘 같은
`ModuleNotFoundError`라 원인을 짚기 어렵다.

```bash
python3 scripts/bootstrap.py          # 환경 준비 (처음 한 번, 맥·윈도우 공통)
.venv/bin/python scripts/normalize_all.py --force
```

팀원은 `대시보드 실행.command`(맥) 또는 `대시보드 실행.bat`(윈도우)만 누르면 된다.
실행기가 `.venv`가 없으면 알아서 만든다.

**이 프로젝트가 밖에서 필요로 하는 것은 파이썬 하나뿐이다.** 나머지(`PyMuPDF`,
`PyYAML`)는 `pip`이 `.venv` 안에 넣는다. 그래서 도구를 여럿 깔 일이 없다.
파이썬이 없을 때는 이렇게 한다.

| | 방법 |
|---|---|
| 맥 | `xcode-select --install` (실행기가 안내한다) |
| 윈도우 | `winget install --exact --id Python.Python.3.12 --scope user` |

`winget`은 윈도우 10(1809 이상)·11에 **기본으로 들어 있어** 따로 깔 필요가 없다.
`--scope user`면 관리자 권한 없이 사용자 계정에만 설치된다. 설치 뒤에는 **새 창**에서
실행해야 한다 — 열려 있던 창의 PATH에는 방금 깐 파이썬이 없다.
`scoop`·`choco`는 그 자체를 먼저 설치해야 하므로 쓰지 않는다.

`PyMuPDF`는 `requirements.txt`에 **버전을 고정**해 두었다. 정규화가 이 라이브러리의
블록·줄 묶음에 그대로 기대고 있어, 버전이 바뀌면 같은 PDF에서 다른 원문이 나온다.
올릴 때는 전권을 재생성하고 `git diff source/`로 확인하며, **ch01이 한 글자도
바뀌지 않아야 한다**(사람 감수를 마친 청크라 회귀 감시 기준으로 쓴다).

## 세션 시작 시 반드시 읽을 문서

| 문서 | 무엇이 들어 있나 |
|---|---|
| `ai_book_translation_pipeline.md` | 파이프라인 기준 문서. 절차·스키마·역할 원칙의 근거 |
| `status.md` | 청크별 진행 단계. 이어서 할 지점을 여기서 판단한다 |
| `guide/tone-and-decision.md` | 톤 지침 + 한국어 표기 규칙 + 결정 기록(append-only) |
| `book-knowledge.md` | 원문 전반의 내용 정리와 참고 지식. 번역 품질을 위한 배경 |
| `structure-map.yaml` | 청크 분할 상태와 구간별 파서 유형 |

작업을 시작하기 전 `glossary/decision-log.jsonl`의 미확인(`ack:false`) 항목을 먼저 확인한다
(`scripts/check_inbox.py`). 세션이 끊긴 사이 사용자가 대시보드에서 내린 결정이 여기 쌓인다.

## 이 프로젝트의 성격

- **파일럿이자 프레임워크**: 이 프로젝트의 궁극적 목적은 팀원들이 각자의 번역 프로젝트를
  수행할 수 있는 **프레임워크를 만드는 것**이다. 핵심 문서들을 공유해 그 기반으로 AI와
  시작하게 한다. 따라서 이 책에만 통하는 임시방편보다, **다른 책에도 그대로 옮겨갈 수 있는
  방식**을 택한다.
- **원서**: The Film Preservation Guide (NFPF, 2004) · 133쪽 · 영 → 한

## 사용자와의 소통 경로

세션 대화 외에 **로컬 서버 대시보드**로도 소통한다(`대시보드 실행.command`).
사용자는 화면에서 용어를 확정하고 문단을 승인·반려한다. 그 결정은 `glossary/decision-log.jsonl`과
`reviews/<청크>-approvals.json`에 쌓이므로, 대화에 나오지 않았더라도 반드시 확인한다.

## 역할 원칙

- **사용자가 하는 일**: 원본 제공, 기본 지침 결정, 선택지 판단, 사람 감수.
- **AI가 하는 일**: 그 외 전부 — 청크 분할, 스크립트 작성·실행, 용어집/TM 관리, 자동 검사,
  변환, 파일 관리. 사용자가 직접 명령어를 치거나 파일을 수동 편집하지 않아도 되게 한다.
- 사용자 확인이 필요한 지점은 **배치로 묶어 한 번에** 제시한다. 세션 중간에 반복해서 끊지 않는다.

## 작업 규율

- **도판은 미리보기로 열리는 모든 md에 같은 자리에 들어가야 한다.** `source/`뿐 아니라
  `chapters/`와 `stages/<청크>/*.md`까지 전부다. 그림은 AI 번역에만이 아니라 **사람이
  감수할 때도 판단 재료**다 — 사진이 빠진 화면만 보고 캡션의 옳고 그름을 판단할 수 없다.
  `scripts/check_manifest.py`가 이를 검사하며 `scripts/gate.py`가 물고 있다.
  경로는 `chapters/` 기준 `../assets/figures/…`로 적는다.
- **`source/`는 언제든 재생성해도 된다**(원문 산출물). 반면 `chapters/`와 `stages/`는
  번역 결과이므로 이런 식으로 덮어쓰지 않는다.
- 원문을 재생성한 뒤에는 이미 번역이 진행된 청크의 변경분을 `git diff source/`로 확인한다.
  문단 수가 달라지면 대조 화면의 1:1 대응이 깨지므로 소급 반영 대상이다(파이프라인 6-1절).
- **커밋을 섞지 않는다**: 사용자가 화면에서 내린 결정의 반영은 "사용자 결정 반영"으로 별도
  커밋한다. 무엇이 사용자의 판단이고 무엇이 AI의 판단인지 이력에서 구분되어야 한다.
  - **`git add -A` 를 쓰지 않는다.** 대시보드가 떠 있는 동안 사용자가 용어를 확정하면
    `glossary/`와 `reviews/`가 조용히 바뀐다. 실제로 두 번 섞여 들어가 커밋을 되돌려
    나눠야 했다. 커밋 전에 `git status` 로 그 두 폴더가 바뀌었는지 먼저 보고,
    바뀌었으면 내 작업분만 경로로 지정해 스테이징한다.
- 결정 기록은 append-only다. 방침을 번복할 때는 기존 항목을 고치지 않고 "언제 왜 번복했는지"를
  새 항목으로 추가한다.
