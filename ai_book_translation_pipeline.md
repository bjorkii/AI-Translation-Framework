# AI 책 번역 파이프라인 — 세션 기준 문서

이 문서는 새 번역 프로젝트를 시작할 때 세션 기본 자료로 제시하는 문서입니다.
AI는 이 문서를 숙지한 뒤, 아래 절차에 따라 사용자에게 **매 단계마다 무엇을 할지 안내하고 선택을 물어야** 합니다.

## 0. 역할 원칙 (가장 중요)

- **사용자가 하는 일**: 원본 PDF 제공, 기본 지침(독자·분야·톤) 제공, 각 단계에서 제시되는 선택지 결정, 사람 감수, 최종 보완 요청.
- **AI가 하는 일**: 그 외 모든 내부 처리 — 청크 분할, 스크립트 작성·실행, 용어집/TM 관리, 자동 검사, CLI 변환, 파일 관리 등. 사용자가 직접 명령어를 치거나 파일을 수동 편집하지 않아도 되게 하는 것이 원칙.
- AI는 매 단계 전환 시 "다음은 무엇을 할지"와 "선택 가능한 옵션"을 먼저 안내하고, 사용자의 결정을 받은 뒤 진행한다. 사용자에게 절차 자체를 설명해달라고 요구하지 않는다.
- 문서 내에서 사용자의 결정이 필요한 지점은 모두 `> 🔵 **[사용자 선택]**` 블록으로 표시한다. 이 표시가 없는 항목은 전부 AI가 자율적으로 처리한다.

### 실행 환경 전제

이 워크플로는 **AI가 코드를 실행할 수 있는 세션**(Cowork 등 클라우드 샌드박스 환경)을 전제로 한다.

- PDF 텍스트 추출(PyMuPDF), OCR(ocrmypdf), 청크 생성, 각종 검증 스크립트, 변환 CLI 실행이 **모두 AI 쪽 환경에서** 이루어진다. 따라서 **팀원 PC에는 별도 설치가 필요 없다** — 이것이 "팀원은 선택만 하고 나머지는 AI가 처리한다"는 역할 원칙이 실제로 성립하는 조건이다.
- 코드 실행이 불가능한 일반 채팅 환경에서는 2절 이하의 상당 부분(스크립트 실행, 자동 검증, 변환)이 작동하지 않는다. 프로젝트를 시작하기 전에 이 조건을 먼저 확인한다.
- 로컬 CLI 환경에서 진행할 경우에는 Python·PyMuPDF·ocrmypdf 등을 각자 설치해야 하므로, IT에 익숙하지 않은 팀원에게는 권장하지 않는다.

### 사용자 선택 지점 한눈에 보기

| 단계 | 무엇을 묻는가 |
|---|---|
| 1. 프로젝트 시작 | 원본 PDF + 예상 독자·분야·톤 지침 |
| 1. 프로젝트 시작 | PDF 텍스트 레이어를 신뢰할지 (재OCR 여부) |
| 1. 프로젝트 시작 | 한국어 표기 규칙 (문장 종결체·인용부호·숫자/단위) |
| 1. 프로젝트 시작 | 분할 단위 확정 (기본값: 챕터) |
| 1. 프로젝트 시작 | git 버전관리 사용 여부 |
| 3. 용어집 관리 | 맞춤법 "원칙 표기 vs 통용 표기" 배치 확인 |
| 6. 워크플로 | 챕터별 배치 확인 (TBD 플래그 + 정규화 불확실 지점) |
| 6. 워크플로 | 사람 감수 |
| 6. 워크플로 | 사람 최종 감수 |
| 8. 최종 산출물 | PDF 변환 방식 선택 |

---

## 1. 프로젝트 시작 단계

1. 사용자가 원본 PDF와 기본 지침(예상 독자, 분야, 전체 톤)을 제공.

> 🔵 **[사용자 선택]** 원본 PDF + 예상 독자·분야·전체 톤 지침 제공.

2. **PDF 텍스트 레이어 신뢰도 확인**: AI가 먼저 원본에서 몇 페이지를 샘플로 뽑아 텍스트 레이어를 기계적으로 스팟체크한다 — 비정상적으로 길게 붙어 있는 어절, 한 단어 내 띄어쓰기 깨짐, 사전에 없는 글자 조합 비율, 특수문자 노이즈 비율 등. 그 결과를 근거로 제시한 뒤 "이 PDF의 텍스트 정보를 신뢰하십니까?"를 묻는다 (막연한 질문 대신 판단 재료를 먼저 제공).
   - **신뢰함** → PyMuPDF 텍스트 레이어 기반으로 2.0절 정규화 진행.
   - **신뢰 안 함** → AI가 전체를 다시 판독한다(페이지 이미지를 직접 판독하거나 ocrmypdf 재실행). 이 경우 **상당한 토큰과 시간이 소모된다는 점을 반드시 사전에 안내**하고 사용자가 확정하게 한다.

> 🔵 **[사용자 선택]** PDF 텍스트 레이어 신뢰 여부 (신뢰하지 않을 경우 전체 재OCR 진행).

3. **한국어 표기 규칙 확정**: 프로젝트 초반에 한 번 정하면 이후 흔들릴 일이 없는 항목들을 먼저 확정한다.
   - 문장 종결체 (~다체 / ~습니다체 등)
   - 인용부호 스타일 (" " / 「 」 / ' ' 등)
   - 숫자·단위 표기 원칙 (1,000 vs 1000 / cm vs 센티미터 등)
   - 확정된 내용은 `guide/tone-and-decision.md`의 "한국어 표기 규칙" 섹션에 기록.

> 🔵 **[사용자 선택]** 한국어 표기 규칙 (문장 종결체·인용부호·숫자/단위).

4. AI가 PDF 구조를 파악한 뒤, 분할 단위를 사용자와 협의 (기본값: 챕터. 챕터가 너무 길면 챕터 내 소단위 분할 여부도 함께 논의). 각주 처리 방식과 챕터 간 상호참조 처리 방침도 이 시점에 함께 확인한다.

> 🔵 **[사용자 선택]** 분할 단위 확정.

5. **버전관리 방식 확인**: AI가 "git으로 버전관리를 하시겠습니까?"라고 물음.
   - **예** → git 저장소를 초기화하고, 이후 모든 단계 전환을 커밋으로 관리 (2.7, 8절 등에서 설명하는 방식).
   - **아니오** → 아래 "8-1. 버전관리 대안 (git 미사용 시)" 방식으로 진행. git/GitHub를 모르는 팀원 다수를 고려한 기본 대안이며, 별도 설치나 학습이 필요 없다.

> 🔵 **[사용자 선택]** git 버전관리 사용 여부.

6. AI가 다음 항목들의 초기 파일/폴더를 생성:
   - `guide/tone-and-decision.md` — 독자·분야·톤 지침 + 한국어 표기 규칙 + 결정 기록(7-1절 참조)
   - `glossary/glossary.yaml` — 빈 용어집(스키마만)
   - `book-knowledge.md` — 빈 책 지식 파일
   - `status.md` — 챕터별 진행 상태 추적표
   - `layout-manifest.yaml` — 위치정보 매니페스트(2.6절)
   - `source/` — 정규화된 **원문** 텍스트 보관 폴더
   - `chapters/` — **번역본** md 보관 폴더
   - `assets/` — 이미지 저장 폴더

---

## 2. 청크(chunk) 생성 — AI가 원본 PDF로부터 자동 수행

### 2.0 텍스트 레이어 정규화 (청크 분할보다 먼저 수행)

PDF에서 뽑은 텍스트를 그대로 쓰면 조판으로 생긴 줄바꿈이 문단 구분처럼 취급되고, 머리말·쪽번호 같은 반복 텍스트가 본문에 섞인다. **분할(2.1)보다 반드시 먼저** 다음 정규화를 수행한다.

**추출 도구 (AI 내부 처리, 사용자에게 묻지 않음)**
- 기본: **PyMuPDF(fitz)** — `page.get_text("dict")`로 텍스트 블록의 좌표(bbox)까지 함께 추출한다. 들여쓰기·좌측 여백·다단 구분을 텍스트 흐름이 아니라 실제 좌표로 판단하기 위함. 이미지 추출·페이지 이미지 렌더링도 같은 라이브러리로 처리해 도구를 통일한다.
- 텍스트 레이어가 없는 스캔본: **ocrmypdf**로 먼저 OCR 텍스트 레이어를 입힌 뒤 동일하게 처리. 이 경우 좌표 정확도가 OCR 품질에 좌우되므로, AI는 "이 PDF는 스캔본이라 좌표 기반 판단의 신뢰도가 떨어질 수 있다"고 사전에 알린다.

**정규화 항목**
1. **줄바꿈 병합**: 조판으로 생긴 줄바꿈(line wrap)을 무력화해 문단을 하나의 흐름으로 복원한다.
   - 1차 기준: 줄이 문장부호로 끝나고 + 다음 줄이 들여쓰기(또는 빈 줄)로 시작하면 문단 구분, 그 외에는 병합.
   - 들여쓰기 신호는 좌표(좌측 여백)로 판단한다. 원본이 들여쓰기 대신 빈 줄로 문단을 구분하는 조판이면 "빈 줄 유무"를 대체 신호로 사용한다.
   - 약어 예외: 마침표 앞이 알려진 약어 패턴(`Mr.`, `cf.`, `etc.` 등)이면 문장 종료로 보지 않는다. 정규식 화이트리스트로 관리.
   - 영문 하이픈 분철(줄 끝 `-`)은 다음 줄과 재결합.
2. **머리말/꼬리말/쪽번호 제거**: 매 페이지 반복되는 러닝헤드·쪽번호 텍스트는 본문에서 제거하고, 페이지 전환 정보는 오직 페이지 마커(2.2)로만 표현한다.
3. **다단(多段) 레이아웃 읽기 순서 정리**: 2단 조판이면 좌측 단을 끝까지 읽은 뒤 우측 단으로 넘어가도록 좌표 기준으로 재정렬한다 (추출기가 가로로 섞어 뽑는 경우가 흔함).
4. **링크 주석 수집**: PDF의 하이퍼링크는 텍스트 레이어가 아니라 **주석(annotation) 레이어**에 있어, 텍스트만 추출하면 링크가 조용히 사라진다. `page.get_links()`로 별도 수집해 마크다운 링크로 재부착하고, 매니페스트(2.6)에 `type: link`로 기록한다.

**규칙 기반 + AI 판독 하이브리드**

정확도는 AI가 페이지를 직접 판독하는 쪽이 높지만, 책 전체를 매번 그렇게 처리하면 정작 번역·감수에 쓸 토큰이 부족해진다. 따라서 비용을 난이도에 비례시킨다.

- **기본**: 위 좌표 기반 규칙으로 저비용에 대부분을 기계적으로 처리.
- **불확실 지점만 AI 판독**: 규칙이 판단하지 못하는 지점(약어 예외, 들여쓰기 신호 부재, 신호 충돌 등)은 **자동 병합하지 말고** `[LINEBREAK-UNCERTAIN]` 마커를 남긴다. 이 지점만 AI가 해당 페이지 이미지를 직접 보고 판단한다.
- **에스컬레이션**: 특정 챕터에서 불확실 마커 비율이 비정상적으로 높으면(복잡한 학술·아카이브 문서에 흔함) 그 챕터 **전체를 페이지 이미지 판독으로 승격**한다. 책 전체가 아니라 어려운 챕터에만 토큰을 집중시킨다.
- 남은 불확실 마커는 6절의 챕터별 배치 확인 때 사용자에게 함께 제시한다.

### 2.1 분할과 중첩
- 정규화된 원문을 협의된 단위(기본: 챕터)로 분할.
- 각 청크 시작부에 **직전 청크의 마지막 1~2문단을 겹쳐서** 포함 (대명사·앞선 언급 참조가 끊기지 않도록, RAG 문서 로더의 청크 오버랩 방식과 동일).
- **겹침 구간은 반드시 마커로 명시**한다. 마커가 없으면 최종 조립 시 같은 문단이 중복 출력되고, 감수자는 이미 승인된 내용을 다시 감수하게 된다.

```
<!-- overlap:start ref=ch03-chunk01 -->
(직전 청크의 마지막 1~2문단 — 이미 승인된 번역)
<!-- overlap:end -->
```

- 최종 조립 스크립트(8절)는 이 구간을 **자동으로 제거**한 뒤 병합한다.
- 감수 패스는 이 구간을 "새 내용 아님"으로 인식해 재감수 대상에서 제외한다.
- 단, 새 청크에서 이 구간의 번역이 직전 청크의 승인본과 달라지면(드리프트) 자동 대조해 flag한다 — 톤 일관성 점검에 부가적으로 활용.

### 2.2 페이지 마커
- 원문 페이지가 바뀌는 지점마다 인라인 마커 삽입: `[원서 p.45]`
- 물리적 "해당 쪽 하단/쪽 넘김"은 마크다운에서 불가능하므로, 페이지 마커는 정보용 표식이자 **원본 대조의 기준점**으로 사용한다 (6절의 원본 재판독 루틴이 이 마커를 이용).

### 2.3 각주·색인·상호참조 — 문서 내 링크 방식
표준 마크다운 문서 내 링크(`[텍스트](#anchor)`)를 사용. pandoc 전용 각주 문법(`[^id]`)은 쓰지 않는다 — 팀원 대다수가 사용하는 Claude 자체 PDF 변환이 이를 인식하지 못하고 본문 중간에 그대로 노출시킬 위험이 있고, 렌더러가 각주를 임의로 챕터 말미로 옮겨버리기 때문.

**앵커 prefix 규칙** (타입별로 최종 LaTeX 변환 시 다른 규칙 적용 가능하도록 구분):
- 각주: `fn-`
- 색인 항목: `idx-`
- 문서 내 상호참조(예: "3장 참조"): `ref-`

**본문 내 마커 형태 (예시, 각주)**:
```
...그는 이렇게 말했다.<a id="back-ch03-012"></a>[¹](#fn-ch03-012)
```

**각주 정의부 (역링크 포함, 필수)**:
```
<a id="fn-ch03-012"></a> **[각주]** 각주 내용...  [↩](#back-ch03-012)
```

**각주 정의의 배치 위치 — 페이지 텍스트 범위 말미**
- 각주 정의는 **챕터 말미에 모으지 않는다.** 해당 각주가 달린 본문이 속한 원서 페이지의 텍스트가 끝나는 지점, 즉 **다음 `[원서 p.N]` 마커 직전**에 앞뒤 빈 줄 하나씩을 두고 배치한다.
- 이것이 가능한 이유: `<a id="...">` + `[텍스트](#anchor)`는 표준 문서 내 링크라 배치 위치가 완전히 자유롭다. 렌더러가 위치를 강제하는 것은 pandoc 각주 문법(`[^id]`)뿐이며, 이 파이프라인은 그 문법을 쓰지 않는다.
- 결과적으로 매 페이지가 "본문 텍스트 → (있다면) 각주/색인 정의 → 페이지 마커"라는 일관된 순서로 반복되므로, 청크 생성 스크립트와 최종 LaTeX 변환 스크립트 모두 이 순서를 가정하고 안정적으로 파싱할 수 있다.

**외부 링크**
- 외부 URL은 페이지 의존성이 없으므로 그대로 옮긴다.
- **링크의 표시 문구는 번역 대상이지만, URL은 절대 수정하지 않는다.**
- 원본 PDF 내부의 페이지 점프 링크는 좌표 기반이라 그대로 쓸 수 없으므로 `ref-`/`fn-`/`idx-` 앵커 체계로 변환한다.

**색인**: 용어집에 등록된 용어가 본문에 등장할 때마다 AI가 자동으로 `idx-` 앵커를 삽입 (색인을 처음부터 새로 만들 필요 없이 용어집이 색인 후보 목록 역할).

### 2.4 표·이미지
- **이미지**: 원본에서 별도 파일로 추출해 `assets/`에 저장. 네이밍 규칙: `챕터-청크순번-청크내이미지순번` (예: `ch03-chunk02-img01.png`) — 캡션은 파일명에 넣지 않는다 (OS별 특수문자/공백 문제, 캡션 수정 시 파일명까지 바뀌는 문제 방지).
  - 본문 삽입 형식: `![캡션 전문](../assets/ch03-chunk02-img01.png)` — 캡션은 alt 텍스트 자리에 넣어 다른 번역 텍스트와 동일하게 감수 흐름을 타게 한다.
- **표**: 구조가 단순하면 마크다운 표로 변환(숫자 데이터는 번역하지 않고 헤더/라벨만 번역). 구조가 복잡하면(병합 셀, 그래픽 요소) 이미지와 동일하게 취급.
- 이미지·표는 모두 매니페스트(2.6)에 레코드로 기록해 누락 검증 대상에 포함시킨다.

### 2.5 원문·번역본 파일 구조

정규화된 원문은 번역 이후에도 계속 보관한다. 원문 대조 감수, 원본 재판독 루틴, 매니페스트 검증, 병기본 생성이 모두 원문을 필요로 하기 때문.

- `source/ch03.md` — 정규화된 **원문** (2.0절 결과물, 이후 수정하지 않음)
- `chapters/ch03.md` — **번역본** (단계가 바뀌어도 파일명 고정)
- **병기본은 파일로 저장하지 않는다.** 원문 대조가 필요한 시점에 위 두 파일을 문단 단위로 엮어 **즉석 생성해 쓰고 버리는 파생물**로 취급한다. 병기본을 별도 파일로 두면 번역문 수정 시 두 곳을 모두 고쳐야 해 반드시 어긋난다.

### 2.6 위치정보 매니페스트 (`layout-manifest.yaml`)

원본 요소의 위치 정보를 인라인 마커로만 두면 "원본의 이미지 12개가 최종본에 다 들어갔는가"를 확인할 방법이 사람 눈밖에 없다. 청크 생성 단계에서 AI가 매니페스트를 자동 생성한다.

**개별 레코드 대상**: 이미지, 표, 각주, 제목, 링크 (개별 식별이 필요한 요소)

```yaml
- id: ch03-chunk02-img01
  type: image            # image | table | footnote | heading | link
  chapter: 03
  chunk: 02
  order: 17              # 청크 내 등장 순서
  source_page: 46
  bbox: [72.0, 310.5, 523.0, 588.2]    # 원본 페이지 좌표 (PyMuPDF)
  asset: assets/ch03-chunk02-img01.png
  status: placed         # placed | missing | pending
```

**본문 텍스트**: 문단마다 개별 레코드를 남기면 책 한 권에 수천 건이 되어 관리가 어려우므로, **페이지 단위 집계**만 기록한다.

```yaml
- type: page_summary
  chapter: 03
  source_page: 46
  paragraph_count: 5
  first_line: "그해 겨울, 촬영은 중단되었다"
```

**활용**
- **누락 검증**: 번역 완료 후 검증 스크립트가 최종 md를 스캔해 `status`를 채우고, `missing` 항목(빠진 이미지·표·링크, 페이지별 문단 수 불일치)을 자동으로 뽑아낸다.
- **원본 재판독**: 6절의 이상 감지 루틴이 좌표까지 바로 찾아갈 수 있다.
- **최종 조판**: 사용자의 XeLaTeX 경로에서 이미지·표를 원본 배치 의도에 맞춰 배치할 근거가 된다.
- **회귀 대조**: 스크립트 수정 전후로 매니페스트가 동일한지 비교(3절).

### 2.7 청크 md의 frontmatter
```yaml
---
chapter: 03
version: 2
stage: draft            # draft / ai_review_1 / ai_review_2 / human_review / final
source_range: "p.45-62"
overlap_prev: true
glossary_snapshot: "glossary_v7.yaml"
---
```
- 파일명은 `chapters/ch03.md` 하나로 고정. 버전 이력은 파일명 증식이 아니라 버전관리 방식(git 커밋 또는 아래 8-1의 타임스탬프 백업)으로 관리.

---

## 3. 용어집(글로서리) 관리

- 형식: 구조화 데이터(YAML/CSV) — 프롬프트에 전체를 넣지 않고, 해당 챕터에 실제 등장하는 용어만 필터링해 주입.
- 필드: `원어 | 번역어 | 풀어쓴 이름 | 표기 규칙 메모 | 품사 | 맥락노트 | 금지된 대안(사유) | 원칙표기 | 채택표기 | 근거(맞춤법 규정 조항 등) | 최초 결정 챕터 | locked`
- **원어(term)는 본문에서 용어를 찾아내는 열쇠**다. 약어의 정식 명칭을 원어 칸에 넣으면(예: `AMIA(Association of Moving Image Archivists)`) 본문의 `AMIA`와 매칭되지 않아 필터링이 깨진다. 정식 명칭은 **풀어쓴 이름(full)** 칸에 두고, 목록에는 `AMIA — Association of Moving Image Archivists`로 함께 표시해 같은 약어의 다른 뜻을 구분한다.
- **`locked: true`**: 인명·지명·기관명 등 초반에 확정한 뒤 잠그는 항목. 잠긴 항목은 AI가 임의로 다르게 옮기거나 변경을 제안할 수 없고, 변경하려면 사용자 승인을 거친다. (고유명사 표기는 뒤늦게 바꾸면 전 챕터를 손봐야 하므로 초기 확정·잠금이 원칙.)
- 필터링 스크립트: AI가 작성 → 사람이 1회 테스트·승인 → 이후 매 단계에서 AI가 자동 실행. 승인된 버전은 고정하고, 수정 시 별도 승인 절차를 거친다.
- **스크립트 회귀 검증**: 스크립트를 수정하면, 확정된 검증 챕터 1개를 재실행해 **직전 스냅샷(8-1절) 및 매니페스트(2.6절)와 대조**한 뒤 승인한다. 별도 테스트 인프라를 만들지 않고 기존 스냅샷·매니페스트를 기대 출력으로 재사용한다. (특히 조립·정규화 스크립트는 틀려도 조용히 틀리므로 이 대조가 유일한 방어선이다.)
- 한국어 맞춤법/외래어 표기 규정을 1차 번역 단계에서 참조. "원칙 표기이나 통용 표기도 허용"되는 경우, 그 챕터 안에서 해당 사례를 모아 **배치로 한 번에** 사용자에게 확인받은 뒤 용어집에 반영 (감수 이후 표기가 바뀌면 재작업이 커지므로 반드시 1차 번역 단계에서 정리).

> 🔵 **[사용자 선택]** "원칙 표기 vs 통용 표기" 채택 여부 (챕터 단위 배치 확인).
- 사용자가 "번역 완료"를 컨펌하면 용어집(데이터)을 엑셀 파일로 출력. 작업 중 원본 포맷은 YAML/CSV로 유지하고 엑셀은 사람이 보는 산출물/보고용으로만 사용.
- **자동 검사기(린터)**: 사람 감수 전 기계적 1차 필터. 다음을 검사한다.
  - 용어집 위반 (원어 등장 문장에서 지정 번역어가 쓰였는지 fuzzy match)
  - **문장 종결체 혼용** (~다체/~습니다체 섞임)
  - **숫자·단위 표기 불일치**
  - **인용부호 혼용**
  - 미해결 `[TBD]` / `[LINEBREAK-UNCERTAIN]` 마커 잔존 여부

---

## 4. Translation Memory (TM)

- 승인된 챕터의 원문-번역문을 문장/절 단위로 축적 (jsonl 등).
- 새 청크 번역 시 유사 문장을 검색해 few-shot 예시로 프롬프트에 주입 → 세션이 바뀌어도 문체 패턴 유지.
- 가장 잘 완성된(만장일치 승인) 챕터 1개를 "톤 앵커"로 매 세션 함께 제공.

---

## 5. 책 지식 축적 문서 (`book-knowledge.md`)

용어집·톤 가이드와 별개로, "이 책이 무엇을 말하는가에 대한 AI의 이해"를 담는 문서.

```markdown
## 핵심 인물/개체
## 챕터별 핵심 논지 요약
## 반복되는 주제/모티프
## AI가 확신하지 못하는 부분 (사용자 확인 필요)
```
- 마지막 섹션이 핵심: AI가 불확실한 이해를 스스로 표시 → 사용자는 전체를 재검토하지 않고 표시된 부분만 확인.
- 3~4챕터마다 주기적 검토를 워크플로에 고정 배치 (잘못된 이해가 누적되는 것을 방지).

---

## 6. 번역·감수 워크플로 (챕터/청크 단위)

1. **1차 번역** (AI) — 용어집 필터링 결과 + TM 유사 문장 + 톤 앵커 챕터를 참조. 맞춤법/외래어 표기 판단은 이 단계에서 배치 확인.
   - **문단 1:1 유지**: 원문의 문단 구조를 임의로 합치거나 쪼개지 않는다. 병기본 생성과 매니페스트 누락 검증이 모두 이 대응 관계를 전제로 한다.
   - **TBD 플래그**: 애매한 용어·뉘앙스를 만나면 조용히 하나를 고르지 말고 `[TBD: A안 or B안 — 왜 애매한지]` 형태로 본문에 표시하고 넘어간다. 선택 자체보다 **선택했다는 사실이 기록되지 않는 것**이 더 위험하기 때문(같은 표현이 뒷 챕터에서 다르게 번역되는 원인).
   - **링크·마커 보존**: 페이지 마커, 각주/색인/상호참조 앵커, overlap 마커, 외부 URL은 이동·삭제하지 않고 그대로 통과시킨다.

> 🔵 **[사용자 선택]** 챕터별 배치 확인 — 맞춤법 원칙/통용 표기, `[TBD]` 플래그, `[LINEBREAK-UNCERTAIN]` 지점을 **한 번에 묶어서** 제시하고 일괄 결정받는다. (세션 중간에 반복해서 끊지 않기 위해 확인 시점을 하나로 합친다.) 확정된 결정은 용어집 또는 `guide/tone-and-decision.md`의 결정 기록에 반영해 같은 딜레마가 반복되지 않게 한다.

2. **AI 감수 1차** — 원문 대조 정확성(누락/오역/숫자·고유명사) 전담 패스. `source/chNN.md`와 번역본을 문단 단위로 엮은 병기본(2.5)을 생성해 대조한다.
   - **이상 감지 시 원본 재판독 루틴**: 문장이 이어지지 않거나, 숫자·고유명사가 이상하거나, 의미불명한 조합이 나타나면 그 즉시 페이지 마커(2.2)와 매니페스트(2.6)를 이용해 **해당 원본 페이지 이미지를 직접 열어 대조·재판단**한다. 추출 텍스트를 그대로 신뢰하지 않는다. 이 루틴은 1차 번역 단계에서도 동일하게 적용한다.
3. **AI 감수 2차** — 용어집 준수(자동 린터 결과 기반 판단) + 문체·톤 일관성 전담 패스.
4. **통과 조건 체크리스트** — 각 감수 패스는 "무엇을 봤고 무엇을 통과/실패시켰는지"를 구조화된 결과(pass/fail + 이유)로 남긴다. 최소 조건:
   - 용어집 위반 0건
   - 원문 대비 누락 없음 (매니페스트 검증 통과: 이미지·표·링크 개수, 페이지별 문단 수 일치)
   - 문장 종결체·숫자·인용부호 일관
   - **미해결 `[TBD]` 0건, `[LINEBREAK-UNCERTAIN]` 0건** (표시만 되고 아무도 안 본 채 최종본까지 흘러가는 것을 막기 위함)
5. **사람 감수** — 사용자가 검토. 위 체크리스트 결과만 훑으면 되도록 한다.

> 🔵 **[사용자 선택]** 사람 감수 — 수정 사항 지적.

6. **AI 보완** — 사람 감수에서 지적된 사항 반영.
7. **사람 최종 감수** — 확정.

> 🔵 **[사용자 선택]** 사람 최종 감수 — 챕터 확정.

### 6-1. 소급 반영 (retrofit)

챕터 10에서 새로 정한 용어·규칙이 챕터 2에는 반영되지 않은 채 남는 것이 장기 번역에서 가장 흔한 실패다.

- 용어집 또는 `guide/tone-and-decision.md`가 업데이트될 때마다, AI는 **이미 확정된 챕터 중 그 용어·규칙이 등장하는 챕터를 자동으로 찾아** `status.md`에 "재검토 필요"로 표시한다.
- 사용자는 이 목록만 보고 반영 여부를 결정한다 (전 챕터를 다시 읽을 필요 없음).

### 6-2. 전체 통합 감수 (unification pass)

챕터별로 각각 통과했더라도, 책 전체로 보면 3장과 8장에서 같은 개념을 다르게 옮겼을 수 있다. 챕터 단위 감수로는 잡히지 않는 영역이다.

- 전체 챕터의 초벌이 완료된 시점에, **번역을 새로 하지 않고 일관성만** 보는 별도 패스를 1회 수행한다 (용어·톤·상호참조·표기 일관성).
- 발견된 불일치는 flag만 남기고, 해당 챕터의 "AI 보완" 단계로 되돌려 처리한다.

### 6-3. 검증 작업의 독립 컨텍스트 실행 (AI 내부 처리)

- 기계적 검증(린터, 매니페스트 대조, 링크·누락 검증, 회귀 대조)과 **원문 대조 감수 패스**는, 실행 환경이 지원하면 **독립 컨텍스트(서브에이전트)로 분리해 수행**한다. 지원하지 않으면 같은 세션에서 순차 실행한다. 어느 쪽이든 사용자가 하는 일은 달라지지 않는다.
- 분리하는 이유는 속도가 아니라 품질이다. 번역을 수행한 컨텍스트가 그대로 감수까지 하면 자신의 판단을 다시 검토하는 셈이라 같은 실수를 공유한다. 번역 과정을 보지 못한 컨텍스트가 원문과 번역문만 놓고 대조할 때 이 문제가 실제로 해소된다.
- **1차 번역과 문체 판단은 분리하지 않는다.** 여러 에이전트로 쪼개면 핸드오프 과정에서 맥락이 유실되고, 병렬 실행은 구독 토큰 한도를 빠르게 소진시킨다. 분리는 검증 쪽에만 적용한다.

---

## 7. 진행 상태 추적 (`status.md`)

- 챕터별로 현재 단계(1차완료/AI감수1/AI감수2/사람감수/AI보완/최종)를 기록. 세션이 바뀔 때 AI는 이 파일을 먼저 읽고 이어서 진행할 지점을 판단한다.
- **재검토 필요 플래그**: 6-1절의 소급 반영 대상으로 지목된 챕터는 확정 상태와 별개로 "재검토 필요(사유: 용어 X 변경)"를 함께 표시한다.

### 7-0. 단계 스냅샷과 단계별 대조

번역본 파일은 단계가 바뀌어도 이름을 유지하며 덮어쓴다. 그러면 "1차 감수가 1차 번역에서 무엇을 고쳤는지"를 화면에서 보여줄 재료가 남지 않는다. 따라서 단계가 끝날 때마다 그 시점의 사본을 `stages/<청크>/<단계>.md`로 남긴다(`scripts/snapshot_stage.py`).

- 대시보드의 **대조 화면**은 이 사본들을 이어 붙여 한 문단마다 `원문 → AI 1차 번역 → AI 1차 감수 → (이후 단계) → 사용자 확인`을 세로로 보여준다.
- 각 단계 블록에는 **직전 단계 대비 변경된 부분**을 단어 단위로 표시한다(추가/삭제).
- 감수 의견은 `reviews/<청크>-review<N>.json`에 문단 번호와 함께 구조화해 저장하고, 화면에서 해당 문단 아래에 붙여 보여준다.
- 사용자는 문단마다 **승인/반려**와 의견 메모를 남길 수 있다. 이 결정은 `reviews/<청크>-approvals.json`에 저장되고 동시에 결정 수신함(7-1절)에 기록되어, AI가 다음 작업을 시작할 때 반려된 문단을 먼저 확인하게 된다.

### 7-1. 사용자 결정 수신함 (`glossary/decision-log.jsonl`)

세션이 끊겨 있는 동안에도 사용자는 대시보드에서 용어를 확정하거나 추가한다. AI가 그 사실을 모른 채 다음 작업을 시작하면, 이미 정해진 것을 다시 묻거나 옛 값으로 번역하게 된다.

- 화면에서 내려진 결정은 모두 append-only 로그에 기록된다: `{시각, 주체, 종류, 용어, 결정값, 메모, ack}`
- 기록에는 주체(`actor`)가 함께 남는다. **사람이 화면에서 내린 결정만 미확인으로 쌓이고**, 대화로 받은 결정을 AI가 대신 입력한 건은 이미 반영된 것이므로 확인 완료로 기록된다. (AI 대리 입력이 미확인으로 쌓이면 사용자에게 "AI가 아직 확인하지 않았다"는 잘못된 신호가 간다.)
- **AI는 작업을 시작할 때마다 이 로그의 미확인(`ack:false`) 항목을 먼저 확인한다.** 반영을 마치면 `scripts/ack_decisions.py --ack "<무엇에 반영했는지>"`로 확인 표시를 남긴다.
- 대시보드 개요 상단에 "AI가 아직 확인하지 않은 결정 N건"이 표시되어, 사용자도 AI가 따라잡았는지 눈으로 확인할 수 있다.
- **커밋 규율**: 사용자가 화면에서 바꾼 내용을 AI 작업과 한 커밋에 섞지 않는다. 내용을 확인한 뒤 "사용자 결정 반영"으로 별도 커밋한다. 섞어 커밋하면 무엇이 사용자의 판단이고 무엇이 AI의 판단인지 이력에서 구분되지 않는다.
- 용어 **삭제**와 **원어 수정**도 같은 로그에 기록된다(`delete`, `rename`). 다만 용어집 YAML에는 삭제 흔적(tombstone)을 남기지 않는다. 이유는 세 가지다. 용어집은 필터링 스크립트·린터·색인 삽입이 소비하는 작업 데이터라 tombstone이 쌓이면 모든 소비자가 매번 걸러내야 하고, 추적은 append-only 로그가 이미 "언제·무엇을·어떤 값에서" 담당하며, git이 삭제 직전의 전체 내용을 보존한다. 즉 **이력은 로그와 git 두 겹으로 남기고 YAML은 현재 상태만 유지한다.**
- 화면의 번역어 입력은 길이·따옴표를 검사한다. 설명 문장이 번역어 칸에 들어가는 사고를 막기 위함이며, 설명은 메모 칸으로 유도한다.

### 7-2. `guide/tone-and-decision.md`

톤 지침과 결정 기록을 한 파일에서 관리한다. "현재 유효한 방침"과 "그 방침이 왜 그렇게 정해졌는지"는 항상 함께 읽혀야 하므로 분리하지 않는다.

```markdown
## 예상 독자 / 분야 / 전체 톤
(사용자가 프로젝트 시작 시 제공)

## 한국어 표기 규칙
- 문장 종결체:
- 인용부호:
- 숫자·단위:

## 결정 기록 (append-only)
- [ch03] 원저자 주와 역자 주를 구분 표기하기로 함. 대안(구분 없이 통합)은 출처 혼동 우려로 배제.
- [ch07] 인용문은 기존 국역본을 따르지 않고 새로 번역하기로 함.
- [ch12] ch07의 결정을 번복 — 저작권 표기가 있는 인용문에 한해 기존 국역본 표기를 따름.
```

- **결정 기록은 append-only**: 기존 항목을 수정하지 않는다. 방침을 번복할 때는 "ch07의 결정을 ch12에서 번복함 + 이유"를 새 항목으로 추가한다. 그래야 왜 바뀌었는지가 추적된다.
- 용어 단위의 판단 근거(왜 이 번역어인지, 어떤 대안을 왜 배제했는지)는 용어집 필드에 남기고, **용어를 넘어서는 프로젝트 방침**만 여기에 기록한다.

---

## 8. 최종 산출물 변환

프로젝트는 1인 진행(각자 처음부터 최종 PDF까지)을 기본으로 하며, PDF 변환 방식은 프로젝트마다 자유롭게 선택한다. AI는 아래 옵션을 제시하고 선택을 받는다.

> 🔵 **[사용자 선택]** PDF 변환 방식 (①~④ 중 선택).

| 옵션 | 설명 | 특징 |
|---|---|---|
| ① Claude 세션 자체 출력 | 세션 내에서 바로 PDF 생성 | 설치 불필요. 각주는 물리적 쪽 하단이 아니라 2.3절 링크 방식 그대로(해당 페이지 텍스트 범위 말미에) 렌더링됨. 대다수 팀원이 선택할 기본 경로. |
| ② pandoc (비-LaTeX 엔진) | `--pdf-engine=weasyprint` 등 | TeXLive 불필요. ①보다 약간 정교한 스타일 제어 가능. |
| ③ VSCode Markdown PDF / Typora / md-to-pdf | 로컬 경량 도구 | Chromium 렌더링 기반, 설치 간단. 감수용 프리뷰에도 적합. |
| ④ pandoc + XeLaTeX (사용자 전용) | 기존 oblivoir 파이프라인 | 각주 정밀 배치, 색인(komkindex), 정교한 조판 가능. 팀원은 사용하지 않음. |

**조립(병합) 규칙**: 최종본을 만들 때 조립 스크립트는 각 청크의 `<!-- overlap:start -->` ~ `<!-- overlap:end -->` 구간을 제거한 뒤 순서대로 병합한다. 병합 후 매니페스트(2.6)와 대조해 이미지·표·링크 개수와 페이지별 문단 수를 검증한다.

**LaTeX 경유 변환 시 (④)**: `fn-`/`idx-`/`ref-` prefix로 구분된 링크·앵커를 AI가 스크립트로 LaTeX 명령으로 치환 (`fn-` → `\footnote{}`, `idx-` → `\index{}`, `ref-` → `\ref{}`). 단순 정규식 1회 치환이 아니라, ① 모든 앵커 정의를 먼저 딕셔너리로 수집 → ② 본문의 링크 마커를 순회하며 치환하는 2단계 스크립트로 처리 (여러 줄·내부 서식이 섞인 각주도 안전하게 처리하기 위함).

---

## 8-1. 버전관리 대안 (git 미사용 시)

git/GitHub에 익숙하지 않은 팀원을 위한 기본 대안. 1절에서 사용자가 "git 미사용"을 선택하면 이 방식으로 진행한다.

- **원본 편집 대상 파일**(`chapters/ch03.md`, `glossary.yaml`, `book-knowledge.md`, `status.md` 등)은 위치·파일명을 그대로 유지하며 계속 덮어쓴다.
- **단계 전환 시점마다**(예: 1차 번역 완료, AI 감수 1차 통과, 사람 감수 반영 직전 등 — 6절의 각 단계가 끝날 때) AI가 자동으로 해당 파일을 로컬 프로젝트 폴더 내 `backup/` 하위에 복사해 스냅샷을 남긴다.
- 백업 파일명 형식: `[원본 파일명]-YYYYMMDD-HHMMSS.md` (예: `ch03-20260904-153012.md`)
- git 사용 시의 "커밋 메시지"에 해당하는 정보(무슨 변경인지)는 같은 이름의 `.txt`로 나란히 남긴다: `ch03-20260904-153012.txt` 안에 "AI 감수 1차 통과, 용어 3건 수정" 식으로 한 줄 기록.
- 사용자는 백업 폴더를 직접 관리할 필요가 없다 — 특정 시점으로 되돌리고 싶으면 AI에게 "몇 시 이전 버전으로 되돌려줘"라고 요청하면 AI가 해당 스냅샷을 찾아 복원한다.
- 이 스냅샷은 3절의 **스크립트 회귀 검증에서 기대 출력**으로도 재사용된다.
- 이 방식은 git처럼 세밀한 diff 비교는 못 하지만, 실수로 덮어쓴 내용을 되돌리거나 이전 상태와 대조하는 목적은 충분히 충족한다.

---

## 9. Skill 공유 구조 (팀 배포 시)

파일럿 프로젝트가 만족스러운 결과를 얻으면, 이 문서의 내용을 **코어 skill**로 만들어 팀에 공유한다.

- **코어 skill (팀 공통, 불변에 가까움)**: 실행 환경 전제, 워크플로 단계 정의, 텍스트 정규화 규칙, 청크/용어집/TM/책지식/매니페스트 파일 스키마, 페이지 마커·각주·색인 링크 규칙, overlap 마커 규칙, 이미지 네이밍 규칙, 용어 필터링·색인 자동삽입·검증 스크립트, 진행상태 추적 포맷, PDF 변환 옵션표.
- **프로젝트별 설정 (각자 채움)**: 톤/독자/분야 지침, 한국어 표기 규칙 선택값, 프로젝트 전용 용어집 인스턴스, 분할 단위 예외 규칙.

팀원 대부분이 md 문법에 익숙하지 않으므로, 코어 skill에 이미지 삽입·굵게/기울임 등 최소 문법만 담은 한 페이지 치트시트를 포함한다.

---

## 10. AI 세션 시작 시 체크리스트

1. 이 문서를 먼저 숙지하고, 코드 실행이 가능한 환경인지 확인(0절 실행 환경 전제).
2. **사용자 결정 수신함**(7-1절)의 미확인 항목을 확인. 세션이 끊긴 사이에 사용자가 내린 결정이 여기 쌓인다. 반영 후 확인 표시를 남긴다.
3. `status.md`를 읽고 진행 지점과 "재검토 필요" 플래그를 파악.
4. `structure-map.yaml`로 청크 분할 상태를 확인 (다른 세션·다른 AI에서 이어받을 때의 기준).
5. `guide/tone-and-decision.md`(방침 + 결정 기록), 용어집, `book-knowledge.md`를 참조 컨텍스트로 로드.
6. 사용자에게 원본 PDF와 기본 지침을 요청 (신규 프로젝트인 경우).
7. PDF 텍스트 신뢰도 스팟체크 → 사용자 확인 → 정규화(2.0) → 청크 생성.
8. 이후 모든 내부 처리(스크립트 실행, 검증, 파일 관리, CLI 변환)는 AI가 직접 수행한다.
9. 선택이 필요한 지점(0절 요약표)에서만 사용자에게 묻되, 챕터별 확인 사항은 배치로 묶어 한 번에 제시한다.
10. 단계가 바뀔 때마다 `scripts/update_status.py`를 실행해 `status.md`를 최신으로 유지한다.
