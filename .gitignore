.DS_Store
__pycache__/
*.pyc
backup/

# Claude 데스크톱 앱이 전달 파일을 놓는 폴더 (프로젝트 산출물 아님)
Claude outputs/

# Claude Code 세션 작업물(계획 파일 등). 결정 사항은 guide/tone-and-decision.md 에 남긴다
.claude/

# AI가 원본 페이지를 눈으로 확인할 때 쓰는 임시 렌더 이미지
intermediate/pagescan/

# 도판 이미지는 PDF에서 다시 뽑을 수 있으므로 버전관리하지 않는다
# (scripts/normalize_all.py --force 로 재생성)
assets/figures/

# 프로젝트 전용 파이썬 환경 (scripts/bootstrap.py 가 만든다)
.venv/

# 매니페스트 조각 (build_manifest.py 가 layout-manifest.yaml 로 합친다)
intermediate/manifest/
